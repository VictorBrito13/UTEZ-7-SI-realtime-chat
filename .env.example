# Archivo de ejemplo para configuración de variables de entorno
# Copia este archivo como .env y ajusta los valores según necesites

# Configuración del Servidor
SERVER_HOST=0.0.0.0    # Usar 0.0.0.0 para escuchar en todas las interfaces
SERVER_PORT=12345      # Puerto para el servidor

# Configuración del Cliente
CLIENT_HOST=127.0.0.1  # Dirección IP del servidor (localhost por defecto)
CLIENT_PORT=12345      # Puerto del servidor

# Base de datos de usuarios (formato JSON)
# Nota: En producción, usar una base de datos real con contraseñas encriptadas
USERS_DB={"admin":"admin123","paola":"dsanchez","jafet":"password2","victor":"test123","daniela":"daniela22"}

# Configuración de RSA
RSA_KEY_SIZE=2048          # Tamaño de la clave RSA en bits
RSA_PUBLIC_EXPONENT=65537  # Exponente público para RSA